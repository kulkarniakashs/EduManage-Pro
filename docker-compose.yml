version: "3.9"

services:
  postgres:
    image: postgres:16
    container_name: edumanage-postgres
    restart: unless-stopped
    environment:
      POSTGRES_DB: edumanage-pro
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: your_db_password
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
